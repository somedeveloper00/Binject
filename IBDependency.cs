namespace Binject {
    
    /// <summary>
    /// Makes the user of the interface known to the dependency injection system as an injectable type. Which you'll be
    /// then able to use either from Unity Inspector or from code.
    /// </summary>
    public interface IBDependency { }
}